# GENERATED PIPELINE FILE - DO NOT EDIT
#
# This file was generated from movielens/ML1M/pipeline.tcl.
# Regenerate with `mise run pipeline:render`.
stages:
  import:
    cmd: lenskit data convert --movielens ml-1m.zip dataset
    deps:
      - ml-1m.zip
    outs:
      - dataset

  stats:
    cmd: lenskit codex sql -D ds_name=ML1M -f ../ml-stats.sql stats.duckdb
    deps:
      - ../ml-stats.sql
      - dataset
    outs:
      - stats.duckdb

  split-random:
    cmd: lenskit codex split random.toml
    wdir: splits
    params:
      - ../../../lenskit.toml:
          - random.seed
    deps:
      - random.toml
      - ../dataset
    outs:
      - random.parquet

  search-als-biased-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 als-biased searches/random/optuna/als-biased
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/als-biased/pipeline.toml
      - ../../models/als-biased/search.toml
    outs:
      - searches/random/optuna/als-biased
      - searches/random/optuna/als-biased.json:
          cache: false
      - searches/random/optuna/als-biased-pipeline.json:
          cache: false

  search-als-implicit-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 als-implicit searches/random/optuna/als-implicit
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/als-implicit/pipeline.toml
      - ../../models/als-implicit/search.toml
    outs:
      - searches/random/optuna/als-implicit
      - searches/random/optuna/als-implicit.json:
          cache: false
      - searches/random/optuna/als-implicit-pipeline.json:
          cache: false

  search-bias-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 bias searches/random/optuna/bias
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/bias/pipeline.toml
      - ../../models/bias/search.toml
    outs:
      - searches/random/optuna/bias
      - searches/random/optuna/bias.json:
          cache: false
      - searches/random/optuna/bias-pipeline.json:
          cache: false

  search-ease-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 ease searches/random/optuna/ease
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/ease/pipeline.toml
      - ../../models/ease/search.toml
    outs:
      - searches/random/optuna/ease
      - searches/random/optuna/ease.json:
          cache: false
      - searches/random/optuna/ease-pipeline.json:
          cache: false

  search-flexmf-bpr-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 flexmf-bpr searches/random/optuna/flexmf-bpr
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/flexmf-bpr/pipeline.toml
      - ../../models/flexmf-bpr/search.toml
    outs:
      - searches/random/optuna/flexmf-bpr
      - searches/random/optuna/flexmf-bpr.json:
          cache: false
      - searches/random/optuna/flexmf-bpr-pipeline.json:
          cache: false

  search-flexmf-explicit-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 flexmf-explicit searches/random/optuna/flexmf-explicit
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/flexmf-explicit/pipeline.toml
      - ../../models/flexmf-explicit/search.toml
    outs:
      - searches/random/optuna/flexmf-explicit
      - searches/random/optuna/flexmf-explicit.json:
          cache: false
      - searches/random/optuna/flexmf-explicit-pipeline.json:
          cache: false

  search-flexmf-logistic-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 flexmf-logistic searches/random/optuna/flexmf-logistic
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/flexmf-logistic/pipeline.toml
      - ../../models/flexmf-logistic/search.toml
    outs:
      - searches/random/optuna/flexmf-logistic
      - searches/random/optuna/flexmf-logistic.json:
          cache: false
      - searches/random/optuna/flexmf-logistic-pipeline.json:
          cache: false

  search-flexmf-warp-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 flexmf-warp searches/random/optuna/flexmf-warp
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/flexmf-warp/pipeline.toml
      - ../../models/flexmf-warp/search.toml
    outs:
      - searches/random/optuna/flexmf-warp
      - searches/random/optuna/flexmf-warp.json:
          cache: false
      - searches/random/optuna/flexmf-warp-pipeline.json:
          cache: false

  search-iknn-explicit-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 iknn-explicit searches/random/optuna/iknn-explicit
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/iknn-explicit/pipeline.toml
      - ../../models/iknn-explicit/search.toml
    outs:
      - searches/random/optuna/iknn-explicit
      - searches/random/optuna/iknn-explicit.json:
          cache: false
      - searches/random/optuna/iknn-explicit-pipeline.json:
          cache: false

  search-iknn-implicit-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 iknn-implicit searches/random/optuna/iknn-implicit
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/iknn-implicit/pipeline.toml
      - ../../models/iknn-implicit/search.toml
    outs:
      - searches/random/optuna/iknn-implicit
      - searches/random/optuna/iknn-implicit.json:
          cache: false
      - searches/random/optuna/iknn-implicit-pipeline.json:
          cache: false

  search-slim-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 slim searches/random/optuna/slim
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/slim/pipeline.toml
      - ../../models/slim/search.toml
    outs:
      - searches/random/optuna/slim
      - searches/random/optuna/slim.json:
          cache: false
      - searches/random/optuna/slim-pipeline.json:
          cache: false

  search-slim-fs-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 slim-fs searches/random/optuna/slim-fs
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/slim-fs/pipeline.toml
      - ../../models/slim-fs/search.toml
    outs:
      - searches/random/optuna/slim-fs
      - searches/random/optuna/slim-fs.json:
          cache: false
      - searches/random/optuna/slim-fs-pipeline.json:
          cache: false

  search-uknn-explicit-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 uknn-explicit searches/random/optuna/uknn-explicit
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/uknn-explicit/pipeline.toml
      - ../../models/uknn-explicit/search.toml
    outs:
      - searches/random/optuna/uknn-explicit
      - searches/random/optuna/uknn-explicit.json:
          cache: false
      - searches/random/optuna/uknn-explicit-pipeline.json:
          cache: false

  search-uknn-implicit-random-optuna:
    cmd: lenskit codex tune --split=splits/random.toml --test-part=0 --sample-count=60 uknn-implicit searches/random/optuna/uknn-implicit
    params:
      - ../../lenskit.toml:
          - tuning.defaults
    deps:
      - splits/random.parquet
      - ../../models/uknn-implicit/pipeline.toml
      - ../../models/uknn-implicit/search.toml
    outs:
      - searches/random/optuna/uknn-implicit
      - searches/random/optuna/uknn-implicit.json:
          cache: false
      - searches/random/optuna/uknn-implicit-pipeline.json:
          cache: false

  run-random-als-biased-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/als-biased-default als-biased
    deps:
      - ../../models/als-biased/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/als-biased-default

  run-random-als-biased-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/als-biased-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/als-biased-optuna-tuned als-biased
    deps:
      - ../../models/als-biased/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/als-biased-pipeline.json
    outs:
      - runs/random/als-biased-optuna-tuned

  run-random-als-implicit-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/als-implicit-default als-implicit
    deps:
      - ../../models/als-implicit/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/als-implicit-default

  run-random-als-implicit-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/als-implicit-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/als-implicit-optuna-tuned als-implicit
    deps:
      - ../../models/als-implicit/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/als-implicit-pipeline.json
    outs:
      - runs/random/als-implicit-optuna-tuned

  run-random-bias-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/bias-default bias
    deps:
      - ../../models/bias/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/bias-default

  run-random-bias-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/bias-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/bias-optuna-tuned bias
    deps:
      - ../../models/bias/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/bias-pipeline.json
    outs:
      - runs/random/bias-optuna-tuned

  run-random-ease-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/ease-default ease
    deps:
      - ../../models/ease/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/ease-default

  run-random-ease-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/ease-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/ease-optuna-tuned ease
    deps:
      - ../../models/ease/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/ease-pipeline.json
    outs:
      - runs/random/ease-optuna-tuned

  run-random-flexmf-bpr-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-bpr-default flexmf-bpr
    deps:
      - ../../models/flexmf-bpr/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/flexmf-bpr-default

  run-random-flexmf-bpr-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/flexmf-bpr-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-bpr-optuna-tuned flexmf-bpr
    deps:
      - ../../models/flexmf-bpr/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/flexmf-bpr-pipeline.json
    outs:
      - runs/random/flexmf-bpr-optuna-tuned

  run-random-flexmf-explicit-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-explicit-default flexmf-explicit
    deps:
      - ../../models/flexmf-explicit/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/flexmf-explicit-default

  run-random-flexmf-explicit-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/flexmf-explicit-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-explicit-optuna-tuned flexmf-explicit
    deps:
      - ../../models/flexmf-explicit/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/flexmf-explicit-pipeline.json
    outs:
      - runs/random/flexmf-explicit-optuna-tuned

  run-random-flexmf-logistic-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-logistic-default flexmf-logistic
    deps:
      - ../../models/flexmf-logistic/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/flexmf-logistic-default

  run-random-flexmf-logistic-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/flexmf-logistic-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-logistic-optuna-tuned flexmf-logistic
    deps:
      - ../../models/flexmf-logistic/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/flexmf-logistic-pipeline.json
    outs:
      - runs/random/flexmf-logistic-optuna-tuned

  run-random-flexmf-warp-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-warp-default flexmf-warp
    deps:
      - ../../models/flexmf-warp/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/flexmf-warp-default

  run-random-flexmf-warp-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/flexmf-warp-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/flexmf-warp-optuna-tuned flexmf-warp
    deps:
      - ../../models/flexmf-warp/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/flexmf-warp-pipeline.json
    outs:
      - runs/random/flexmf-warp-optuna-tuned

  run-random-iknn-explicit-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/iknn-explicit-default iknn-explicit
    deps:
      - ../../models/iknn-explicit/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/iknn-explicit-default

  run-random-iknn-explicit-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/iknn-explicit-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/iknn-explicit-optuna-tuned iknn-explicit
    deps:
      - ../../models/iknn-explicit/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/iknn-explicit-pipeline.json
    outs:
      - runs/random/iknn-explicit-optuna-tuned

  run-random-iknn-implicit-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/iknn-implicit-default iknn-implicit
    deps:
      - ../../models/iknn-implicit/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/iknn-implicit-default

  run-random-iknn-implicit-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/iknn-implicit-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/iknn-implicit-optuna-tuned iknn-implicit
    deps:
      - ../../models/iknn-implicit/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/iknn-implicit-pipeline.json
    outs:
      - runs/random/iknn-implicit-optuna-tuned

  run-random-popular-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/popular-default popular
    deps:
      - ../../models/popular/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/popular-default

  run-random-slim-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/slim-default slim
    deps:
      - ../../models/slim/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/slim-default

  run-random-slim-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/slim-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/slim-optuna-tuned slim
    deps:
      - ../../models/slim/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/slim-pipeline.json
    outs:
      - runs/random/slim-optuna-tuned

  run-random-slim-fs-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/slim-fs-default slim-fs
    deps:
      - ../../models/slim-fs/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/slim-fs-default

  run-random-slim-fs-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/slim-fs-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/slim-fs-optuna-tuned slim-fs
    deps:
      - ../../models/slim-fs/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/slim-fs-pipeline.json
    outs:
      - runs/random/slim-fs-optuna-tuned

  run-random-uknn-explicit-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/uknn-explicit-default uknn-explicit
    deps:
      - ../../models/uknn-explicit/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/uknn-explicit-default

  run-random-uknn-explicit-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/uknn-explicit-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/uknn-explicit-optuna-tuned uknn-explicit
    deps:
      - ../../models/uknn-explicit/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/uknn-explicit-pipeline.json
    outs:
      - runs/random/uknn-explicit-optuna-tuned

  run-random-uknn-implicit-default:
    cmd: lenskit codex generate --ds-name=ML1M --split=splits/random.toml -o runs/random/uknn-implicit-default uknn-implicit
    deps:
      - ../../models/uknn-implicit/pipeline.toml
      - dataset
      - splits/random.parquet
    outs:
      - runs/random/uknn-implicit-default

  run-random-uknn-implicit-optuna-tuned:
    cmd: lenskit codex generate --pipeline-file searches/random/optuna/uknn-implicit-pipeline.json --ds-name=ML1M --split=splits/random.toml -o runs/random/uknn-implicit-optuna-tuned uknn-implicit
    deps:
      - ../../models/uknn-implicit/pipeline.toml
      - dataset
      - splits/random.parquet
      - searches/random/optuna/uknn-implicit-pipeline.json
    outs:
      - runs/random/uknn-implicit-optuna-tuned

  collect-metrics:
    cmd: lenskit codex collect metrics -S run-summary.csv -U run-user-metrics.parquet -L runs/manifest.csv
    deps:
      - runs/manifest.csv
      - runs/random/als-biased-default
      - runs/random/als-biased-optuna-tuned
      - runs/random/als-implicit-default
      - runs/random/als-implicit-optuna-tuned
      - runs/random/bias-default
      - runs/random/bias-optuna-tuned
      - runs/random/ease-default
      - runs/random/ease-optuna-tuned
      - runs/random/flexmf-bpr-default
      - runs/random/flexmf-bpr-optuna-tuned
      - runs/random/flexmf-explicit-default
      - runs/random/flexmf-explicit-optuna-tuned
      - runs/random/flexmf-logistic-default
      - runs/random/flexmf-logistic-optuna-tuned
      - runs/random/flexmf-warp-default
      - runs/random/flexmf-warp-optuna-tuned
      - runs/random/iknn-explicit-default
      - runs/random/iknn-explicit-optuna-tuned
      - runs/random/iknn-implicit-default
      - runs/random/iknn-implicit-optuna-tuned
      - runs/random/popular-default
      - runs/random/slim-default
      - runs/random/slim-optuna-tuned
      - runs/random/slim-fs-default
      - runs/random/slim-fs-optuna-tuned
      - runs/random/uknn-explicit-default
      - runs/random/uknn-explicit-optuna-tuned
      - runs/random/uknn-implicit-default
      - runs/random/uknn-implicit-optuna-tuned
    outs:
      - run-summary.csv
      - run-user-metrics.parquet
