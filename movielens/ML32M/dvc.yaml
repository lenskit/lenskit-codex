# GENERATED PIPELINE FILE - DO NOT EDIT
#
# This file was generated from movielens/ML32M/pipeline.tcl.
# Regenerate with `mise run pipeline:render`.
stages:
  import:
    cmd: lenskit data convert --movielens ml-32m.zip dataset
    deps:
      - ml-32m.zip
    outs:
      - dataset

  stats:
    cmd: lenksit codex sql -D ds_name=ML32M -f ../ml-stats.sql stats.duckdb
    deps:
      - ../ml-stats.sql
      - dataset
    outs:
      - stats.duckdb

  run-temporal-als-biased-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/als-biased-default als-biased
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/als-biased-default

  run-temporal-als-implicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/als-implicit-default als-implicit
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/als-implicit-default

  run-temporal-bias-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/bias-default bias
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/bias-default

  run-temporal-ease-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/ease-default ease
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/ease-default

  run-temporal-flexmf-bpr-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/flexmf-bpr-default flexmf-bpr
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-bpr-default

  run-temporal-flexmf-explicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/flexmf-explicit-default flexmf-explicit
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-explicit-default

  run-temporal-flexmf-logistic-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/flexmf-logistic-default flexmf-logistic
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-logistic-default

  run-temporal-flexmf-warp-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/flexmf-warp-default flexmf-warp
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-warp-default

  run-temporal-iknn-explicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/iknn-explicit-default iknn-explicit
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/iknn-explicit-default

  run-temporal-iknn-implicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/iknn-implicit-default iknn-implicit
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/iknn-implicit-default

  run-temporal-popular-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/popular-default popular
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/popular-default

  run-temporal-uknn-explicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/uknn-explicit-default uknn-explicit
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/uknn-explicit-default

  run-temporal-uknn-implicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/temporal.toml -o runs/temporal/uknn-implicit-default uknn-implicit
    deps:
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/uknn-implicit-default

  collect-metrics:
    cmd: lenskit codex collect metrics -S run-summary.csv -U run-user-metrics.parquet -L runs/manifest.csv
    deps:
      - runs/temporal/temporal-als-biased-default
      - runs/temporal/temporal-als-implicit-default
      - runs/temporal/temporal-bias-default
      - runs/temporal/temporal-ease-default
      - runs/temporal/temporal-flexmf-bpr-default
      - runs/temporal/temporal-flexmf-explicit-default
      - runs/temporal/temporal-flexmf-logistic-default
      - runs/temporal/temporal-flexmf-warp-default
      - runs/temporal/temporal-iknn-explicit-default
      - runs/temporal/temporal-iknn-implicit-default
      - runs/temporal/temporal-popular-default
      - runs/temporal/temporal-uknn-explicit-default
      - runs/temporal/temporal-uknn-implicit-default
    outs:
      - run-summary.csv
      - run-user-metrics.parquet
