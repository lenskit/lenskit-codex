# GENERATED PIPELINE FILE - DO NOT EDIT
#
# This file was generated from movielens/ML32M/pipeline.tcl.
# Regenerate with `mise run pipeline:render`.
stages:
  import:
    cmd: lenskit data convert --movielens ml-32m.zip dataset
    deps:
      - ml-32m.zip
    outs:
      - dataset

  stats:
    cmd: lenksit codex sql -D ds_name=ML32M -f ../ml-stats.sql stats.duckdb
    deps:
      - ../ml-stats.sql
      - dataset
    outs:
      - stats.duckdb

  split-random:
    cmd: lenskit codex split random.toml
    wdir: splits
    deps:
      - random.toml
      - ../dataset
    outs:
      - random.parquet

  run-random-als-biased-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/als-biased-default als-biased
    deps:
      - splits/random
    outs:
      - runs/random/als-biased-default

  run-random-als-implicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/als-implicit-default als-implicit
    deps:
      - splits/random
    outs:
      - runs/random/als-implicit-default

  run-random-bias-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/bias-default bias
    deps:
      - splits/random
    outs:
      - runs/random/bias-default

  run-random-ease-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/ease-default ease
    deps:
      - splits/random
    outs:
      - runs/random/ease-default

  run-random-flexmf-bpr-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/flexmf-bpr-default flexmf-bpr
    deps:
      - splits/random
    outs:
      - runs/random/flexmf-bpr-default

  run-random-flexmf-explicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/flexmf-explicit-default flexmf-explicit
    deps:
      - splits/random
    outs:
      - runs/random/flexmf-explicit-default

  run-random-flexmf-logistic-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/flexmf-logistic-default flexmf-logistic
    deps:
      - splits/random
    outs:
      - runs/random/flexmf-logistic-default

  run-random-flexmf-warp-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/flexmf-warp-default flexmf-warp
    deps:
      - splits/random
    outs:
      - runs/random/flexmf-warp-default

  run-random-iknn-explicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/iknn-explicit-default iknn-explicit
    deps:
      - splits/random
    outs:
      - runs/random/iknn-explicit-default

  run-random-iknn-implicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/iknn-implicit-default iknn-implicit
    deps:
      - splits/random
    outs:
      - runs/random/iknn-implicit-default

  run-random-popular-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/popular-default popular
    deps:
      - splits/random
    outs:
      - runs/random/popular-default

  run-random-uknn-explicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/uknn-explicit-default uknn-explicit
    deps:
      - splits/random
    outs:
      - runs/random/uknn-explicit-default

  run-random-uknn-implicit-default:
    cmd: lenskit codex generate --ds-name=ML32M --split=splits/random -o runs/random/uknn-implicit-default uknn-implicit
    deps:
      - splits/random
    outs:
      - runs/random/uknn-implicit-default

  collect-metrics:
    cmd: lenskit codex collect metrics -S run-summary.csv -U run-user-metrics.parquet -L runs/manifest.csv
    deps:
      - runs/random/random-als-biased-default
      - runs/random/random-als-implicit-default
      - runs/random/random-bias-default
      - runs/random/random-ease-default
      - runs/random/random-flexmf-bpr-default
      - runs/random/random-flexmf-explicit-default
      - runs/random/random-flexmf-logistic-default
      - runs/random/random-flexmf-warp-default
      - runs/random/random-iknn-explicit-default
      - runs/random/random-iknn-implicit-default
      - runs/random/random-popular-default
      - runs/random/random-uknn-explicit-default
      - runs/random/random-uknn-implicit-default
    outs:
      - run-summary.csv
      - run-user-metrics.parquet
