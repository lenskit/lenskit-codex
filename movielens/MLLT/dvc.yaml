# GENERATED PIPELINE FILE - DO NOT EDIT
#
# This file was generated from movielens/MLLT/pipeline.tcl.
# Regenerate with `mise run pipeline:render`.
stages:
  import:
    cmd: lenskit data convert --movielens ml-latest.zip dataset
    deps:
      - ml-latest.zip
    outs:
      - dataset

  stats:
    cmd: lenskit codex sql -D ds_name=MLLT -f ../ml-stats.sql stats.duckdb
    deps:
      - ../ml-stats.sql
      - dataset
    outs:
      - stats.duckdb

  run-temporal-als-biased-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/als-biased-default als-biased
    deps:
      - ../../models/als-biased/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/als-biased-default

  run-temporal-als-implicit-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/als-implicit-default als-implicit
    deps:
      - ../../models/als-implicit/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/als-implicit-default

  run-temporal-bias-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/bias-default bias
    deps:
      - ../../models/bias/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/bias-default

  run-temporal-flexmf-bpr-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/flexmf-bpr-default flexmf-bpr
    deps:
      - ../../models/flexmf-bpr/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-bpr-default

  run-temporal-flexmf-explicit-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/flexmf-explicit-default flexmf-explicit
    deps:
      - ../../models/flexmf-explicit/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-explicit-default

  run-temporal-flexmf-logistic-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/flexmf-logistic-default flexmf-logistic
    deps:
      - ../../models/flexmf-logistic/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/flexmf-logistic-default

  run-temporal-iknn-explicit-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/iknn-explicit-default iknn-explicit
    deps:
      - ../../models/iknn-explicit/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/iknn-explicit-default

  run-temporal-iknn-implicit-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/iknn-implicit-default iknn-implicit
    deps:
      - ../../models/iknn-implicit/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/iknn-implicit-default

  run-temporal-popular-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/popular-default popular
    deps:
      - ../../models/popular/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/popular-default

  run-temporal-slim-fs-default:
    cmd: lenskit codex generate --ds-name=MLLT --split=splits/temporal.toml -o runs/temporal/slim-fs-default slim-fs
    deps:
      - ../../models/slim-fs/pipeline.toml
      - dataset
      - splits/temporal.toml
    outs:
      - runs/temporal/slim-fs-default

  collect-metrics:
    cmd: lenskit codex collect metrics -S run-summary.csv -U run-user-metrics.parquet -L runs/manifest.csv
    deps:
      - runs/manifest.csv
      - runs/temporal/als-biased-default
      - runs/temporal/als-implicit-default
      - runs/temporal/bias-default
      - runs/temporal/flexmf-bpr-default
      - runs/temporal/flexmf-explicit-default
      - runs/temporal/flexmf-logistic-default
      - runs/temporal/iknn-explicit-default
      - runs/temporal/iknn-implicit-default
      - runs/temporal/popular-default
      - runs/temporal/slim-fs-default
    outs:
      - run-summary.csv
      - run-user-metrics.parquet
